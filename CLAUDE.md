# ForeverFree Project — Claude Instructions

## Git workflow
After completing any code changes, always:
1. Run `flutter test` from the app directory and confirm all tests pass
2. Commit the changes locally with a descriptive commit message
3. Push to GitHub (`git push origin main`)

Do this automatically without waiting to be asked. Never commit or push if tests are failing.
