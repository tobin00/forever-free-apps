import 'package:flutter/material.dart';

class TrickyFlagsQuizScreen extends StatelessWidget {
  const TrickyFlagsQuizScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO(Phase 3): Implement tricky flags quiz — questions drawn from
    // TrickyFlagGroupsData.all, distractors are other members of the same group.
    return Scaffold(
      appBar: AppBar(title: const Text('Tricky Flags')),
      body: const Center(child: Text('Tricky Flags Quiz — coming in Phase 3')),
    );
  }
}
