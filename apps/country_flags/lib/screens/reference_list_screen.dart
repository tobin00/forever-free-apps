import 'package:flutter/material.dart';

class ReferenceListScreen extends StatelessWidget {
  const ReferenceListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO(Phase 3): Implement reference list — alphabetical list of all ~197
    // countries with flag image + name, search bar at top for filtering.
    return Scaffold(
      appBar: AppBar(title: const Text('All Flags')),
      body: const Center(child: Text('Reference List — coming in Phase 3')),
    );
  }
}
